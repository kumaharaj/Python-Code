pipeline {
agent none
stages {
    stage('Build') {
        agent any     
            }
        }
        steps {
            sh 'python -m py_compile variables.py'
            stash(name: 'compiled-results',includes: 'sources/*.py*')
    
          }
      }  
 
